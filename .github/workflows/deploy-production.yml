name: Deploy to Production

on:
  push:
    branches:
      - main

jobs:
  deploy:
    name: Deploy Production
    runs-on: ubuntu-latest
    environment: production

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build
        env:
          NODE_ENV: production

      - name: Deploy to Vercel (Production)
        uses: amondnet/vercel-action@v25
        with:
          vercel-token: ${{ secrets.vcp_85LbiHRpPikTeFLJSWLXtj2erm9RgC6bb3WIikgSrDRcnOxfv72AtmCM }}
          vercel-org-id: ${{ secrets.team_CGaWk0TlE4KJWnzHku4UFjrU }}
          vercel-project-id: ${{ secrets.prj_TLzH0GBs85T1iDtXZo7IEUcbSPFI }}
          vercel-args: "--prod"
          working-directory: ./

      - name: Notify on success
        if: success()
        run: echo "✅ Production deploy successful!"

      - name: Notify on failure
        if: failure()
        run: echo "❌ Production deploy failed!"
        # Add Slack/email webhook here if needed
